import { definePluginEntry } from "openclaw/plugin-sdk/plugin-entry";

export default definePluginEntry({
  id: "kling-ai-global",
  name: "Kling AI Global",
  description: "Kling AI remote MCP and generation skills for OpenClaw.",
  register() {},
});
